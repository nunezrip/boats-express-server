var express = require('express');
var app = express();

const PORT = 3000;

var jsonData = { cunt: 12, message: 'Hey' };

app.get('/', (req, res) => {
	res.sendFile(__dirname + '/index.html', err => {
		if (err) {
			res.status(300).send(err);
		}
	});
});

app.get('/data', (req, res) => {
	res.json(jsonData);
});

app.listen(PORT, () => {
	console.log(`listening on port ${PORT}`);
});
